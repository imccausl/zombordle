{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "CI"
  ],
  "globalDependencies": [
    "tsconfig.json"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "build-tokens"
      ],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "lib",
        "out/**"
      ]
    },
    "build-tokens": {
      "outputs": [
        "lib"
      ]
    },
    "lint": {
      "dependsOn": [
        "build-tokens"
      ]
    },
    "//#lint-css": {},
    "typecheck": {},
    "test": {
      "dependsOn": [
        "build-tokens"
      ],
      "outputs": [
        "app/coverage/**/*"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "src/**/*.tsx",
        "src/**/*.ts",
        "test/**/*.ts",
        "test/**/*.tsx"
      ]
    }
  }
}
