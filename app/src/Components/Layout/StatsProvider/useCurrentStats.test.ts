import { act, renderHook } from '@testling-library/react'

import { useCurrentStats } from './useCurrentStats'

describe('useCurrentStats', () => {
    it.todo('should return the stats for the current word length')
    it.todo('should set stats for the current word length')
    it.todo('should return current streak for the current word lenght')
    it.todo(
        'should return correct guess distribution for the current word length',
    )
    it.todo('should return the max streak for the current word length')
})
